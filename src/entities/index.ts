import { Emails } from './emails.entity';
import { Painels } from './painels.entity';
import { PainelsNames } from './painelsnames.entity';
import { RPMLink } from './rpmlink.entity';
import { Schedule } from './schedule.entity';
import { User } from './user.entity';
import { VideoCall } from './videocall.entity';

const Entities = {
  Emails,
  Painels,
  PainelsNames,
  RPMLink,
  Schedule,
  User,
  VideoCall,
};

export default Entities;
